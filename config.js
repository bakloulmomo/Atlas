/**
 * Configurazione ATLAS - Aggiorna API_BASE per deploy (ngrok, produzione)
 * Per dev locale: lascia "http://localhost:8000"
 * Per GitHub Pages + ngrok: metti qui il tuo URL ngrok (es. https://xxxx.ngrok-free.app)
 */
window.ATLAS_CONFIG = {
  API_BASE: "http://127.0.0.1:4040/inspect/http"
};

